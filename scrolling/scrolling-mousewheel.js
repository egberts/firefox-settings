

// sets acceleration factor if mouse wheel.acceleration.start > -1
user_pref("mousewheel.acceleration.factor", 10);

user_pref("mousewheel.acceleration.start", 0);

// sets the vertical step size
user_pref("mousewheel.default.delta_multiplier_y", 85);

// if the CSS line height is smaller than this value in
// pixels, each scroll click will scroll this amount
user_pref("mousewheel.min_line_scroll_amount", 10);

